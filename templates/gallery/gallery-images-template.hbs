{{#each data}}
  <div class="col-md-3">
    <a href="#gallery/{{id}}" onclick="loadDetails('gallery/{{id}}','details-template','content')">
      <img class="img-thumbnail mb-3" src="{{imagepath}}" alt="{{name}}" />
    </a>
  </div>
{{/each}}