<div class="container py-5 overflow-auto">
  {{#each data}}

    <h1 class="py-3">{{name}}</h1>
    <img
      src="{{thumbpath}}"
      class="float-start rounded-4 me-3"
      alt="{{name}}"
    />
    {{{description}}}

  {{/each}}
</div>